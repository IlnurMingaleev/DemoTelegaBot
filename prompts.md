1)Контекст задачи: Каким образом я могу сделать телеграм бот с напоминаниями в курсор не используя API платных нейронок для распознавания речи. Я хочу чтобы ты подумал над возможными форматами задания напоминаний через голосовое сообщение в телеграм. И использовал хорошее и качественное бесплатное решение для перевода голосового сообщения в текст. С последующим выделением даты и времени на которое телеграм бот поставит уведомление. После постановки напоминания бот должен оповестить пользоавтеля о том на какое дату и время поставил уведомление О статусе напоминания. Временную зону пользователя.  В  ответном сообщении пользователю внутри бота. Я хочу чтоб ты использовал язык Python для создания данного телеграм бота. В первой итерации мы будем делать телеграм бот в режиме polling для локального запуска далее я хотел бы чтобы ты добавил возможность перевести телеграм бот в режим WEBHooks для деплоя телеграм бота на сервер. Я хочу чтоб также раписал Readme файл для пользователя с тем какие шаги требуются от него. В первую очередь это получить API ключ бота и chatID пользователя чтобы отправлять ему уведомления с напоминаниями.  Задача на данном этапе: Это распланировать ее выполнение с помощью создания вспомогательных [Subagents] задействуя комманду  /create-subagent  Также если необходимо создать [Skills] для выполнения задач используя команду /create-skill  Использовать команду /create-rule для создания [Rules] проекта и связать все это в один RoadMap используя инструмент [Hooks] из команды /create-hook  для создания телеграм бота с напоминаниями из контекста задачи Используя инструменты которые мы создадим с помощью комманд.


Ты — ревьюер деплоя. Проверь мой проект Telegram-бота на готовность к деплою в Railway.

КРИТИЧЕСКОЕ ОГРАНИЧЕНИЕ:
- НИЧЕГО НЕ МЕНЯЙ в репозитории: не редактируй файлы, не создавай новые, не удаляй, не ставь зависимости, не запускай миграции, не делай коммиты.
- Разрешено только читать файлы и анализировать.
- Если чего-то не хватает — просто перечисли, что именно и где, и предложи точные правки, но не выполняй их.

ЦЕЛЬ:
 Мне нужно: 1) чеклист обязательных требований, 2) поиск потенциальных проблем, которые могут сломать деплой/работу после установки.

ЧТО ПРОВЕРИТЬ (выдать результат структурированно):
1) Точка входа
- где стартует бот (например, main.py), режим polling/webhook, корректность выбора режима через env.
- нет ли бизнес-логики в entrypoint (желательно только запуск).

2) Переменные окружения Railway
- какие env обязательны (BOT_TOKEN, MODE, WEBHOOK_URL/PORT/SECRET и т.п.), где читаются, что будет если пустые.
- есть ли .env.example и что в нём (без реальных секретов).

3) requirements / зависимости
- есть ли requirements.txt, адекватные зависимости (aiogram/pyrogram/apscheduler/aiosqlite/…).
- нет ли зависимостей, которые в Railway часто ломаются (сборка wheels, системные либы) — просто отметь риски.

4) Procfile / Start command / runtime
- есть ли Procfile или Railway-совместимая команда запуска (python -m … / python main.py).
- не привязан ли запуск к локальным путям/Windows-специфике.

5) Порты и вебхук (если используется)
- слушает ли приложение PORT из env (Railway обычно даёт PORT).
- проверка webhook secret header (если webhook-режим), HTTPS URL, отсутствие хардкода.

6) Хранилище и БД
- если SQLite: путь к файлу, персистентность (Railway FS может быть ephemeral), риски потери данных.
- если есть альтернатива (Postgres) — просто упомяни рекомендации, но ничего не внедряй.

7) Файлы/кэш/временные директории
- где создаются аудио/кэш/лог-файлы; не пишется ли в папки без прав.
- есть ли .gitignore для .env, *.db, audio_cache, __pycache__.

8) Логи и обработка ошибок
- используется ли logging, нет ли print в прод-коде.
- сообщения об ошибках пользователю: кратко и по-русски.

9) Сетевые ограничения/таймауты
- долгие операции (STT, запросы) — есть ли timeouts, retry; потенциальные подвисания.

ФОРМАТ ОТЧЁТА:
- Раздел “✅ Ок”
- Раздел “⚠️ Риск/замечание” (что именно, где в коде, почему это риск в Railway)
- Раздел “❌ Проблема” (что сломает деплой/работу)
- Раздел “Рекомендованные точечные правки” (списком, с указанием файлов/функций)
- В конце: минимальный “Railway checklist” из 8–15 пунктов

Если чего-то не можешь проверить из кода (например, настройки Railway UI), явно напиши какие 2–5 вопросов/значений мне нужно посмотреть в Railway.